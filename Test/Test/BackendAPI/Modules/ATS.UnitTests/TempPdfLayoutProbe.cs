// Scratch file - safe to delete.
//
// This briefly held a throwaway probe that rendered the application form preview
// PDF to PNG so the employer field grid could be inspected after the supervisor
// email field was added. The inspection is done; the probe is gone. The file
// itself could not be removed from this environment (delete was permission
// denied), so it is emptied instead. Delete it.
