<UserManagementDialogShell Title="Edit role"
                           Subtitle="Update role details"
                           SectionTitle="Role details"
                           SectionHelper="Review and update this role"
                           Icon="@Icons.Material.Outlined.EditNote"
                           OnClose="Cancel">
    <ChildContent>
        <MudForm @ref="EditRoleForm">
            <div class="um-field">
                <label for="um-edit-role-name">Role name <span class="um-required">*</span></label>
                <div class="um-input-wrap um-single-line-wrap">
                    <MudTextField @bind-Value="EditRole.roleName"
                                  InputId="um-edit-role-name"
                                  Placeholder="e.g. Branch supervisor"
                                  Required="true"
                                  RequiredError="Role Name is required"
                                  Variant="Variant.Outlined"
                                  Margin="Margin.Dense"
                                  MaxLength="100"
                                  Adornment="Adornment.Start"
                                  AdornmentIcon="@Icons.Material.Outlined.AdminPanelSettings" />
                </div>
            </div>

            <div class="um-field">
                <label for="um-edit-role-description">Role description <span class="um-required">*</span></label>
                <div class="um-input-wrap um-description-wrap">
                    <MudTextField @bind-Value="EditRole.Description"
                                  InputId="um-edit-role-description"
                                  Placeholder="What access does this role grant?"
                                  Required="true"
                                  RequiredError="Description is required"
                                  Variant="Variant.Outlined"
                                  Margin="Margin.Dense"
                                  Lines="4"
                                  Immediate="true"
                                  MaxLength="100"
                                  Adornment="Adornment.Start"
                                  AdornmentIcon="@Icons.Material.Outlined.Article" />
                </div>
                <div class="um-character-count" aria-live="polite">@(EditRole.Description?.Length ?? 0) / 100</div>
            </div>
        </MudForm>
    </ChildContent>
    <FooterContent>
        <button type="button" class="um-button um-button-cancel" @onclick="Cancel">Cancel</button>
        <button type="button" class="um-button um-button-save" @onclick="Submit">Save changes</button>
    </FooterContent>
</UserManagementDialogShell>
