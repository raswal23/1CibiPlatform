<UserManagementDialogShell Title="Add submenu"
                           Subtitle="Create a new submenu entry"
                           SectionTitle="Submenu details"
                           SectionHelper="Name and describe the submenu"
                           SecondarySectionTitle="Availability"
                           SecondarySectionHelper="Turn the submenu on or off"
                           Icon="@Icons.Material.Outlined.Menu"
                           OnClose="Cancel">
    <ChildContent>
        <MudForm @ref="AddSubMenuForm">
            <div class="um-field">
                <label for="um-submenu-name">Submenu name <span class="um-required">*</span></label>
                <div class="um-input-wrap um-single-line-wrap">
                    <MudAutocomplete T="string"
                                     @bind-Value="SubMenu.SubMenuName"
                                     InputId="um-submenu-name"
                                     Placeholder="Select a submenu name"
                                     Required="true"
                                     RequiredError="SubMenu Name is required"
                                     Variant="Variant.Outlined"
                                     Margin="Margin.Dense"
                                     MaxLength="100"
                                     MaxItems="50"
                                     SearchFunc="SearchSubMenus"
                                     ToStringFunc="@(x => x)"
                                     ResetValueOnEmptyText="false"
                                     Clearable="false"
                                     Dense="true"
                                     OpenOnFocus="true"
                                     Adornment="Adornment.Start"
                                     AdornmentIcon="@Icons.Material.Outlined.Menu"
                                     OpenIcon="@Icons.Material.Filled.KeyboardArrowDown"
                                     CloseIcon="@Icons.Material.Filled.KeyboardArrowUp" />
                </div>
            </div>

            <div class="um-field">
                <label for="um-submenu-description">Submenu description <span class="um-required">*</span></label>
                <div class="um-input-wrap um-description-wrap">
                    <MudTextField @bind-Value="SubMenu.Description"
                                  InputId="um-submenu-description"
                                  Placeholder="What does this submenu provide?"
                                  Required="true"
                                  RequiredError="Description is required"
                                  Variant="Variant.Outlined"
                                  Margin="Margin.Dense"
                                  Lines="4"
                                  Immediate="true"
                                  MaxLength="100"
                                  Adornment="Adornment.Start"
                                  AdornmentIcon="@Icons.Material.Outlined.Article" />
                </div>
                <div class="um-character-count" aria-live="polite">@(SubMenu.Description?.Length ?? 0) / 100</div>
            </div>
        </MudForm>
    </ChildContent>
    <SecondaryContent>
        <div class="um-field">
            <label>Status</label>
            <div class="um-status-row">
                <div class="um-status-copy">
                    <p>Submenu @(SubMenu.IsActive ? "active" : "inactive")</p>
                    <span>Available to assign to roles once saved</span>
                </div>
                <div class="um-status-control">
                    <span class="um-status-badge @(SubMenu.IsActive ? string.Empty : "is-off")">
                        @(SubMenu.IsActive ? "Active" : "Inactive")
                    </span>
                    <button type="button"
                            class="um-toggle @(SubMenu.IsActive ? string.Empty : "is-off")"
                            role="switch"
                            aria-checked="@SubMenu.IsActive.ToString().ToLowerInvariant()"
                            aria-label="Toggle submenu status"
                            @onclick="ToggleStatus">
                        <span></span>
                    </button>
                </div>
            </div>
        </div>
    </SecondaryContent>
    <FooterContent>
        <button type="button" class="um-button um-button-cancel" @onclick="Cancel">Cancel</button>
        <button type="button" class="um-button um-button-save" @onclick="Submit">Save</button>
    </FooterContent>
</UserManagementDialogShell>
